# 数阔夜校 · 讲稿配合版

双击 `index.html` 用 Edge 或 Chrome 打开，无需安装或构建。图片与两段视频随包保存，打开真实 GitHub 项目需要联网。

## 11 屏，与讲稿配合

1. 开场：还有多少事情，因为“我不会”而没有尝试？
2. AIGC：从内容，到流程，再到能力。
3. 视频 Skill：播放真实作品，下面保留需求到成片的流程。
4. 保存能力：在“成片”和“方法”之间切换两幅完整场景。
5. Remotion：真实知识动画，与从目标到做出的路径。
6. Vibe Coding：实际调整画面，保留“我要什么 / 结果对吗 / 往哪里改”三个判断。
7. 发现技术：问方向、找项目、读懂用，每一步对应一幅图。
8. GitHub：借用已有能力，并可打开真实团队项目。
9. Git：Commit、Diff、Branch 三种独立演练，按现场需要选择。
10. 团队能力库：把方法本递给同事，点亮内部能力库。
11. 找借改存：四个字切换四幅主画面，回到“获得新能力的能力”。

详细经历与解释由讲者口述，页面避免整段讲稿。逐屏对应和讲解建议见 `讲者手册.html`。

## 操作

- 方向键 / Page Up / Page Down 翻页；空格下一页（按钮和表单控件保留原生操作）。
- F 全屏；G 目录；N 讲解提示；Home / End 开场 / 结束；Esc 关闭面板或取消拖拽。
- AIGC、发现技术、Git、找借改存：悬停关键字即可切换整幅场景，也可点击或键盘定位。
- Vibe Coding：在主题卡上左右移动，或拖动下方滑杆。
- 方法交接：把方法本拖到右侧同事，也可直接点击；再点方法本可重新演示。
- 触屏支持点选与滑杆，键盘支持 Tab / Enter / 方向键；系统开启减少动态效果时禁用转场与倾身。
- 视频点击播放，翻页自动暂停；临时换视频仅当前会话有效，长期替换修改 `config.js`。

N 面板会显示在当前投屏上。正式讲解时关闭，或在另一设备阅读讲者手册。

## 演练与真实素材

两段真实视频原样复制，未转码：视频 Skill 成片 43.306 秒，Remotion 知识动画 119.017 秒。

其他互动为本地教学演示，刷新重置，不连接在线 AI、不生成新视频、不操作真实 Git 仓库。Git 存档、差异和分支为三个独立概念样例，页面状态不跨样例共享。

六位同学由内置 imagegen 按用户参考图生成，共十二种姿势。素材保存在 `assets/night-school-classmates-clean.png` 和 `assets/night-school-new-classmates.png`，通过 CSS 显示对应区域。提示词见 `素材生成说明.md`。

## 文件

`index.html` 内容；`app.js` 导航与视频；`story.js` 主要互动；`speaker.js` 讲稿补充场景；`tokens.css` / `style.css` 基础样式；`story.css` / `focus.css` / `speaker.css` 叙事与场景样式；`config.js` 视频路径。

转移电脑时请解压整个展示包，再打开 HTML，不要只复制 index.html。
